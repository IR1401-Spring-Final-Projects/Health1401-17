# from elasticsearch import Elasticsearch


# class ElasticsearchResult:
#     def __init__(ELASTIC_PASSWORD):
#         self.client = Elasticsearch("https://localhost:9200", ca_certs="/path/to/http_ca.crt",
#          basic_auth=("elastic", ELASTIC_PASSWORD))

#     def is_indexed():
#         resp = es.get(index="health-doc", id=1)
#         print(resp['_source'])

# initial = ElasticsearchResult('svjksgjnj')
# initial.is_indexed()